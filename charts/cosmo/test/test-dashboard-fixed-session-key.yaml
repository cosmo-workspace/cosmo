testSpec:
  desc: fixed session key

domain: example.com

dashboard:
  session:
    secretKeys:
      immutable: false
      COOKIE_HASHKEY: hash_key
      COOKIE_BLOCKKEY: block_key
      COOKIE_SESSION_NAME: sess_name
